# Saamu Tailors Backend Runtime Dependencies
Django>=5.0,<6.0
djangorestframework>=3.15.0
djangorestframework-simplejwt>=5.3.1
django-cors-headers>=4.3.0
python-dotenv>=1.0.1
psycopg2-binary>=2.9.9
# Phase 17: PDF report export rendering
reportlab>=4.2,<5.0
# Local production serving: Waitress (WSGI server) + WhiteNoise (static files
# and the compiled React build served by Django itself).
waitress>=3.0.0
whitenoise>=6.12.0
